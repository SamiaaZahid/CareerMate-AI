import 'package:flutter/material.dart';

/// Shared app colors: purple stays as the main UI color;
/// orange is used as a brand accent for logo marks and key actions.
class AppColors {
  AppColors._();

  static const Color primaryPurple = Color(0xFF54309C);
  static const Color accentOrange = Color(0xFFFF8A00);
}
